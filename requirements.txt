numpy>=1.21.0
pyaudio>=0.2.11
pyserial>=3.5
pvporcupine>=3.0.0

# OpenCV - install via system package (recommended):
# sudo apt-get install python3-opencv python3-opencv-contrib

# Required for loading .env files
python-dotenv>=1.0.0

# YOLO (Ultralytics) for object detection
# Much simpler than TensorFlow Lite - works with any Python 3.6+
# Model will auto-download on first run (~6MB for yolo11n.pt)
ultralytics>=8.0.0

# Note: Gesture detection now uses simple image analysis (no MediaPipe/OpenPose needed)
# Works with any Python version and doesn't require additional dependencies

# Optional packages:
# openai>=1.0.0  # For full vision system (not needed for bindieselsimple.py)
# mediapipe  # For person detection (alternative to OpenCV)

