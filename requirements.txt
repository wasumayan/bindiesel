numpy>=1.21.0
pyaudio>=0.2.11
pyserial>=3.5
pvporcupine>=3.0.0

# OpenCV - install via system package (recommended):
# sudo apt-get install python3-opencv python3-opencv-contrib

# Required for loading .env files
python-dotenv>=1.0.0

# TensorFlow Lite for object detection
# Note: tflite-support requires Python 3.7-3.9
# If using Python 3.10+, you may need to use a virtual environment with Python 3.9
tflite-support>=0.4.2,<0.4.4
protobuf>=3.18.0,<4

# Optional packages:
# openai>=1.0.0  # For full vision system (not needed for bindieselsimple.py)
# mediapipe  # For person detection (alternative to OpenCV)

